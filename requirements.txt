# Stash panel — install into WINDOWS Python:
#   python.exe -m pip install -r requirements.txt
#
# QtMultimedia is not in PySide6-Essentials as of 6.11 (it moved to Addons),
# and the panel needs it for audition + video preview.
PySide6-Essentials>=6.9
PySide6-Addons>=6.9
Pillow>=11
numpy>=2
soundfile>=0.13
imageio-ffmpeg>=0.6
pywin32>=306
